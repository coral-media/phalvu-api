<html lang="en">
  <head>
    <title>{{ title }}</title>
  </head>
  <body>
    <div class="error-container">
      <h1 class="text-danger">{{ code }}</h1>
      <h2>{{ title }}</h2>
      <p class="lead">{{ detail }}</p>
    </div>
  </body>
</html>